export enum COLOR_ATTRIB {
  default = 'default',
  primary = 'primary',
  secondary = 'secondary'
}

export enum BUTTON_VARIANTS {
  default = 'text',
  contained = 'contained',
}

export enum WEEK_DAYS {
  sunday,
  monday,
  tuesday,
  wednesday,
  thursday,
  friday,
  saturday,
}

export enum LICENSE_DIGITS {
  zero,
  one,
  two,
  three,
  four,
  five,
  six,
  seven,
  eight,
  nine,
}